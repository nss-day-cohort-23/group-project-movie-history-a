{{!-- for each card passed in --}}

{{!-- 
Movie name {{this.movie_title}}
Movie poster, if available {{this.movie_poster_full_URL}} else placeholder image
Year released {{this.movie_year}}
List of major actors {{this.movie_cast}}

if there is a property of watched and it will print as a watched movie with class of watched, rate btn, delete btn, "watched"
An integer rating of 1-5 {{this.rating}}

else if there is a property of uid, print as unwatched movie with class of unwatched, delete btn, "not yet watched", watched btn

else print as DB movie with class of dbMovie, add to watchlistbtn

--}}